
task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    archiveClassifier.set("sources")
}

task javadocJar(type: Jar) {
    from javadoc
    archiveClassifier.set("javadoc")
}

task writeNewPom {
    doLast {
        pom {
            project {
                inceptionYear '2019'
                licenses {
                    license {
                        name 'The Apache Software License, Version 2.0'
                        url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                        distribution 'repo'
                    }
                }
            }
        }.writeTo("./pom.xml")
    }
}

// maven plugin
publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.decoded'
            artifactId = 'polldancer'
            version = '1.0'

            from components.java
        }

        mavenJava(MavenPublication) {
            artifactId = 'polldancer'
            from components.java
            artifact sourcesJar
            artifact javadocJar
            pom {
                name = 'Poll Dancer'
                description = 'Uses non-blocking polling for triggering callbacks on different threads.'
                url = 'https://github.com/decoded4620/PollDancer.git'

                licenses {
                    license {
                        name = 'Apache 2.0'
                        url = 'https://www.apache.org/licenses/LICENSE-2.0.html'
                    }
                }
                developers {
                    developer {
                        id = 'barcher'
                        name = 'Bow Archer'
                        email = 'decoded4620@github'
                    }
                }
                scm {
                    connection = 'scm:git:git://github.com/decoded4620/PollDancer.git'
                    developerConnection = 'scm:git:ssh://github.com/decoded4620/PollDancer.git'
                    url = 'https://github.com/decoded4620/PollDancer'
                }
            }
        }
    }
}
