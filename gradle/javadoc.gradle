javadoc {
    setTitle("Poll Dancer Java Docs")
    setDestinationDir(new File(project.getProjectDir().getPath() + '/docs/javadoc'))
    setFailOnError(true)
    options {
        noTimestamp = true
    }

}